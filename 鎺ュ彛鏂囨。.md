# 合同智能审批系统 - API接口文档

## 1. 基础信息

### 1.1 接口概述

本文档定义了合同智能审批系统的所有后端API接口，遵循RESTful设计规范。每个接口均提供请求与响应案例，案例数据与 `contract.sql` 种子数据保持一致。

### 1.2 接口前缀

前端配置的基础路径为：`/api`（通过axios的baseURL配置）

**实际请求路径** = `baseURL` + 文档中定义的路径

例如：文档路径 `/user/login` → 实际请求路径 `/api/user/login`

> 本文案例统一采用实际请求路径（含 `/api` 前缀）展示。

### 1.3 响应格式

#### 成功响应

```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": 1715234567890
}
```

#### 分页响应

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [...],
    "total": 100,
    "page": 1,
    "size": 20
  },
  "timestamp": 1715234567890
}
```

#### 错误响应

```json
{
  "code": 400,
  "message": "参数错误",
  "data": null,
  "timestamp": 1715234567890
}
```

### 1.4 错误码说明

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

## 2. 用户管理接口

### 2.1 用户登录

**POST** `/user/login`

**使用位置**：`Login.vue` - 登录表单提交按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

响应体（扁平化结构，前端直接使用）：

| 字段 | 类型 | 说明 |
|------|------|------|
| token | string | JWT令牌 |
| id | number | 用户ID |
| username | string | 用户名 |
| name | string | 真实姓名 |
| role | string | 角色编码 |
| roleName | string | 角色名称 |
| roleId | number | 角色ID |
| department | string | 部门 |

**请求示例**：

```http
POST /api/user/login
Content-Type: application/json

{
  "username": "admin",
  "password": "123456"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJhZG1pbiIsInJvbGVJZCI6MX0.xxxx",
    "id": 1,
    "username": "admin",
    "name": "管理员",
    "role": "ADMIN",
    "roleName": "系统管理员",
    "roleId": 1,
    "department": "技术部"
  },
  "timestamp": 1715234567890
}
```

### 2.2 用户登出

**POST** `/user/logout`

**使用位置**：`MainLayout.vue` - 右上角用户菜单中的"退出登录"按钮

无请求参数

**请求示例**：

```http
POST /api/user/logout
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 2.3 获取当前用户信息

**GET** `/user/info`

**使用位置**：`MainLayout.vue` - 页面刷新时自动调用，用于恢复登录状态

响应体：同登录接口的字段结构

**请求示例**：

```http
GET /api/user/info
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9.xxxx",
    "id": 1,
    "username": "admin",
    "name": "管理员",
    "role": "ADMIN",
    "roleName": "系统管理员",
    "roleId": 1,
    "department": "技术部"
  },
  "timestamp": 1715234567890
}
```

### 2.4 获取用户列表

**GET** `/user/list`

**使用位置**：`User.vue` - 页面加载时调用，展示用户列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码，默认1 |
| size | number | 否 | 每页条数，默认20 |
| username | string | 否 | 用户名模糊搜索 |
| name | string | 否 | 姓名模糊搜索 |
| roleId | number | 否 | 角色ID筛选 |
| department | string | 否 | 部门筛选 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 用户ID |
| list[].username | string | 用户名 |
| list[].name | string | 真实姓名 |
| list[].roleId | number | 角色ID |
| list[].roleName | string | 角色名称 |
| list[].department | string | 部门 |
| list[].phone | string | 手机号 |
| list[].email | string | 邮箱 |
| list[].status | number | 状态 |
| list[].createTime | string | 创建时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/user/list?page=1&size=10&roleId=2
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 2,
        "username": "fawuzhuguan",
        "name": "法务主管",
        "roleId": 2,
        "roleName": "法务主管",
        "department": "法务部",
        "phone": "13800138001",
        "email": "fawuzhuguan@example.com",
        "status": 1,
        "createTime": "2026-07-01 10:00:00"
      },
      {
        "id": 3,
        "username": "fawuzhuanyuan",
        "name": "法务专员",
        "roleId": 3,
        "roleName": "法务专员",
        "department": "法务部",
        "phone": "13800138002",
        "email": "fawuzhuanyuan@example.com",
        "status": 1,
        "createTime": "2026-07-01 10:00:00"
      }
    ],
    "total": 2,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 2.5 新增用户

**POST** `/user`

**使用位置**：`User.vue` - "新增用户"弹窗中点击"确定"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |
| name | string | 是 | 真实姓名 |
| roleId | number | 是 | 角色ID |
| department | string | 否 | 部门 |
| phone | string | 否 | 手机号 |
| email | string | 否 | 邮箱 |

**请求示例**：

```http
POST /api/user
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "username": "zhangsan",
  "password": "123456",
  "name": "张三",
  "roleId": 3,
  "department": "法务部",
  "phone": "13900139001",
  "email": "zhangsan@example.com"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 6,
    "username": "zhangsan",
    "name": "张三",
    "roleId": 3,
    "roleName": "法务专员",
    "department": "法务部"
  },
  "timestamp": 1715234567890
}
```

### 2.6 更新用户

**PUT** `/user/{id}`

**使用位置**：`User.vue` - 用户列表中点击"编辑"按钮，修改后提交

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 否 | 真实姓名 |
| roleId | number | 否 | 角色ID |
| department | string | 否 | 部门 |
| phone | string | 否 | 手机号 |
| email | string | 否 | 邮箱 |
| status | number | 否 | 状态 |

**请求示例**：

```http
PUT /api/user/3
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "name": "法务专员",
  "department": "法务部",
  "phone": "13900139002",
  "email": "fawuzhuanyuan@example.com",
  "status": 1
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 3,
    "username": "fawuzhuanyuan",
    "name": "法务专员",
    "roleId": 3,
    "department": "法务部"
  },
  "timestamp": 1715234567890
}
```

### 2.7 删除用户

**DELETE** `/user/{id}`

**使用位置**：`User.vue` - 用户列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/user/6
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 2.8 重置密码

**PUT** `/user/{id}/resetPassword`

**使用位置**：`User.vue` - 用户列表中点击"重置密码"按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| password | string | 是 | 新密码 |

**请求示例**：

```http
PUT /api/user/3/resetPassword
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "password": "123456"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 3. 角色管理接口

### 3.1 获取角色列表

**GET** `/role/list`

**使用位置**：`Role.vue` - 页面加载时调用，展示角色列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 角色ID |
| list[].roleName | string | 角色名称 |
| list[].roleCode | string | 角色编码 |
| list[].description | string | 角色描述 |
| list[].status | number | 状态 |
| list[].userCount | number | 用户数量 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/role/list?page=1&size=10
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "roleName": "系统管理员",
        "roleCode": "ADMIN",
        "description": "系统配置、用户管理、权限分配，拥有全部功能",
        "status": 1,
        "userCount": 1
      },
      {
        "id": 2,
        "roleName": "法务主管",
        "roleCode": "LEGAL_MANAGER",
        "description": "审核规则配置、模板管理、审核报告查看、团队管理",
        "status": 1,
        "userCount": 1
      }
    ],
    "total": 5,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 3.2 获取角色详情

**GET** `/role/{id}`

**使用位置**：`Role.vue` - 点击"编辑权限"按钮时调用，获取角色权限信息

响应体：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 角色ID |
| roleName | string | 角色名称 |
| roleCode | string | 角色编码 |
| description | string | 角色描述 |
| status | number | 状态 |
| menuIds | number[] | 权限菜单ID列表 |

**请求示例**：

```http
GET /api/role/2
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "roleName": "法务主管",
    "roleCode": "LEGAL_MANAGER",
    "description": "审核规则配置、模板管理、审核报告查看、团队管理",
    "status": 1,
    "menuIds": [1, 2, 3, 4, 13, 14, 15, 16, 17, 5, 18, 19, 6, 20, 21, 7, 8, 12, 24, 25, 26, 27]
  },
  "timestamp": 1715234567890
}
```

### 3.3 新增角色

**POST** `/role`

**使用位置**：`Role.vue` - "新增角色"弹窗中点击"确定"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| roleName | string | 是 | 角色名称 |
| roleCode | string | 是 | 角色编码 |
| description | string | 否 | 角色描述 |
| menuIds | number[] | 否 | 权限菜单ID列表 |

**请求示例**：

```http
POST /api/role
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "roleName": "财务审核员",
  "roleCode": "FINANCE_REVIEWER",
  "description": "负责合同财务条款审核",
  "menuIds": [1, 2, 4, 13, 14, 17, 7]
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 6,
    "roleName": "财务审核员",
    "roleCode": "FINANCE_REVIEWER"
  },
  "timestamp": 1715234567890
}
```

### 3.4 更新角色

**PUT** `/role/{id}`

**使用位置**：`Role.vue` - 编辑角色弹窗中点击"确定"按钮提交表单

请求体：同新增角色

**请求示例**：

```http
PUT /api/role/2
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "roleName": "法务主管",
  "roleCode": "LEGAL_MANAGER",
  "description": "审核规则配置、模板管理、审核报告查看、团队管理",
  "menuIds": [1, 2, 3, 4, 13, 14, 15, 16, 17, 5, 18, 19, 6, 20, 21, 7, 8, 12]
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "roleName": "法务主管",
    "roleCode": "LEGAL_MANAGER"
  },
  "timestamp": 1715234567890
}
```

### 3.5 删除角色

**DELETE** `/role/{id}`

**使用位置**：`Role.vue` - 角色列表中点击"删除"按钮，确认后删除（管理员角色不可删除）

**请求示例**：

```http
DELETE /api/role/6
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 3.6 分配角色菜单权限

**POST** `/role/{id}/menus`

**使用位置**：`Role.vue` - 编辑权限弹窗中点击"确定"按钮提交权限配置

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| menuIds | number[] | 是 | 菜单ID列表 |

**请求示例**：

```http
POST /api/role/3/menus
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "menuIds": [1, 2, 4, 13, 14, 16, 17, 5, 18, 6, 20, 7]
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 4. 菜单管理接口

### 4.1 获取菜单列表

**GET** `/menu/list`

**使用位置**：`Menu.vue` - 页面加载时调用，展示菜单列表表格

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 菜单ID |
| parentId | number | 父菜单ID |
| menuName | string | 菜单名称 |
| menuType | string | 菜单类型 |
| path | string | 路由路径 |
| component | string | 组件路径 |
| icon | string | 图标 |
| permission | string | 权限标识 |
| sortOrder | number | 排序号 |
| status | number | 状态 |

**请求示例**：

```http
GET /api/menu/list
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "parentId": 0,
      "menuName": "工作台",
      "menuType": "MENU",
      "path": "/",
      "component": "views/Dashboard.vue",
      "icon": "HomeFilled",
      "permission": "dashboard:view",
      "sortOrder": 1,
      "status": 1
    },
    {
      "id": 2,
      "parentId": 0,
      "menuName": "合同管理",
      "menuType": "CATALOG",
      "path": "/contract",
      "component": "",
      "icon": "Document",
      "permission": "",
      "sortOrder": 2,
      "status": 1
    }
  ],
  "timestamp": 1715234567890
}
```

### 4.2 获取菜单树

**GET** `/menu/tree`

**使用位置**：`Role.vue` - 新增/编辑角色时，弹窗中展示权限选择树

响应体：树形结构，包含children字段

**请求示例**：

```http
GET /api/menu/tree
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "parentId": 0,
      "menuName": "工作台",
      "menuType": "MENU",
      "path": "/",
      "icon": "HomeFilled",
      "children": []
    },
    {
      "id": 2,
      "parentId": 0,
      "menuName": "合同管理",
      "menuType": "CATALOG",
      "path": "/contract",
      "icon": "Document",
      "children": [
        {
          "id": 3,
          "parentId": 2,
          "menuName": "合同上传",
          "menuType": "MENU",
          "path": "/contract/upload",
          "icon": "Upload",
          "children": []
        },
        {
          "id": 4,
          "parentId": 2,
          "menuName": "合同列表",
          "menuType": "MENU",
          "path": "/contract/list",
          "icon": "Document",
          "children": []
        }
      ]
    }
  ],
  "timestamp": 1715234567890
}
```

### 4.3 获取角色菜单

**GET** `/menu/byRole`

**使用位置**：`Login.vue` - 登录成功后调用，获取当前用户有权限的菜单列表；`MainLayout.vue` - 刷新页面恢复登录状态时调用

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| roleId | number | 是 | 角色ID |

**请求示例**：

```http
GET /api/menu/byRole?roleId=2
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "parentId": 0,
      "menuName": "工作台",
      "menuType": "MENU",
      "path": "/",
      "component": "views/Dashboard.vue",
      "icon": "HomeFilled",
      "permission": "dashboard:view",
      "sortOrder": 1,
      "status": 1
    },
    {
      "id": 2,
      "parentId": 0,
      "menuName": "合同管理",
      "menuType": "CATALOG",
      "path": "/contract",
      "component": "",
      "icon": "Document",
      "permission": "",
      "sortOrder": 2,
      "status": 1
    }
  ],
  "timestamp": 1715234567890
}
```

### 4.4 获取菜单详情

**GET** `/menu/{id}`

**使用位置**：`Menu.vue` - 点击"编辑"按钮时调用，获取菜单详细信息

**请求示例**：

```http
GET /api/menu/13
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 13,
    "parentId": 2,
    "menuName": "合同归档",
    "menuType": "MENU",
    "path": "/contract/archive",
    "component": "views/contract/Archive.vue",
    "icon": "FolderOpened",
    "permission": "contract:archive",
    "sortOrder": 3,
    "status": 1
  },
  "timestamp": 1715234567890
}
```

### 4.5 新增菜单

**POST** `/menu`

**使用位置**：`Menu.vue` - "新增菜单"弹窗中点击"确定"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| parentId | number | 是 | 父菜单ID |
| menuName | string | 是 | 菜单名称 |
| menuType | string | 是 | 菜单类型 |
| path | string | 否 | 路由路径 |
| component | string | 否 | 组件路径 |
| icon | string | 否 | 图标 |
| permission | string | 否 | 权限标识 |
| sortOrder | number | 否 | 排序号 |

**请求示例**：

```http
POST /api/menu
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "parentId": 2,
  "menuName": "合同借阅",
  "menuType": "MENU",
  "path": "/contract/borrow",
  "component": "views/contract/Borrow.vue",
  "icon": "Document",
  "permission": "contract:borrow",
  "sortOrder": 4
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 28,
    "parentId": 2,
    "menuName": "合同借阅"
  },
  "timestamp": 1715234567890
}
```

### 4.6 更新菜单

**PUT** `/menu/{id}`

**使用位置**：`Menu.vue` - 编辑菜单弹窗中点击"确定"按钮提交表单

请求体：同新增菜单

**请求示例**：

```http
PUT /api/menu/13
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "parentId": 2,
  "menuName": "合同归档",
  "menuType": "MENU",
  "path": "/contract/archive",
  "component": "views/contract/Archive.vue",
  "icon": "FolderOpened",
  "permission": "contract:archive",
  "sortOrder": 3
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 13,
    "menuName": "合同归档"
  },
  "timestamp": 1715234567890
}
```

### 4.7 删除菜单

**DELETE** `/menu/{id}`

**使用位置**：`Menu.vue` - 菜单列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/menu/28
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 5. 合同管理接口

### 5.1 上传合同

**POST** `/contracts/upload`

**使用位置**：`Upload.vue` - 合同上传页面点击"提交"按钮上传合同文件和信息

Content-Type: `multipart/form-data`

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 合同文件 |
| contractName | string | 是 | 合同名称 |
| contractType | string | 是 | 合同类型 |
| partyA | string | 是 | 甲方 |
| partyB | string | 是 | 乙方 |
| amount | number | 是 | 合同金额 |
| signDate | string | 否 | 签订日期 |
| startDate | string | 否 | 开始日期 |
| endDate | string | 否 | 结束日期 |

**请求示例**：

```http
POST /api/contracts/upload
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="contractName"

XX科技有限公司采购合同
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="contractType"

采购合同
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="partyA"

XX科技有限公司
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="partyB"

XX供应商
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="amount"

500000
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="signDate"

2026-07-01
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file"; filename="purchase-contract.pdf"
Content-Type: application/pdf

<binary>
------WebKitFormBoundary7MA4YWxkTrZu0gW--
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 6,
    "contractNo": "CT20260706006",
    "contractName": "XX科技有限公司采购合同",
    "status": "UPLOADED",
    "uploadUserName": "法务主管",
    "uploadTime": "2026-07-06 14:00:00"
  },
  "timestamp": 1715234567890
}
```

### 5.2 获取合同列表

**GET** `/contracts`

**使用位置**：`List.vue` - 合同列表页面加载时调用，展示合同列表表格；`Dashboard.vue` - 工作台展示合同概览；`Archive.vue` - 合同归档页面展示已归档合同

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| contractNo | string | 否 | 合同编号 |
| contractName | string | 否 | 合同名称 |
| contractType | string | 否 | 合同类型 |
| status | string | 否 | 状态 |
| riskLevel | string | 否 | 风险等级 |
| uploadUserId | number | 否 | 上传人ID |
| startTime | string | 否 | 上传开始时间 |
| endTime | string | 否 | 上传结束时间 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 合同ID |
| list[].contractNo | string | 合同编号 |
| list[].contractName | string | 合同名称 |
| list[].contractType | string | 合同类型 |
| list[].partyA | string | 甲方 |
| list[].partyB | string | 乙方 |
| list[].amount | number | 合同金额 |
| list[].status | string | 状态 |
| list[].riskLevel | string | 风险等级 |
| list[].riskScore | number | 风险评分 |
| list[].uploadUserName | string | 上传人姓名 |
| list[].uploadTime | string | 上传时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/contracts?page=1&size=10&status=REVIEWING
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "contractNo": "CT20260706001",
        "contractName": "XX科技有限公司采购合同",
        "contractType": "采购合同",
        "partyA": "XX科技有限公司",
        "partyB": "XX供应商",
        "amount": 500000.00,
        "status": "REVIEWING",
        "riskLevel": "HIGH",
        "riskScore": 75,
        "uploadUserName": "法务主管",
        "uploadTime": "2026-07-06 09:30:00"
      },
      {
        "id": 2,
        "contractNo": "CT20260706002",
        "contractName": "XX服务合同",
        "contractType": "服务合同",
        "partyA": "XX科技有限公司",
        "partyB": "XX服务公司",
        "amount": 80000.00,
        "status": "REVIEWING",
        "riskLevel": "MEDIUM",
        "riskScore": 45,
        "uploadUserName": "法务专员",
        "uploadTime": "2026-07-06 10:15:00"
      }
    ],
    "total": 2,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 5.3 获取合同详情

**GET** `/contracts/{id}`

**使用位置**：`Detail.vue` - 合同列表中点击"查看详情"按钮进入详情页时调用

响应体：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 合同ID |
| contractNo | string | 合同编号 |
| contractName | string | 合同名称 |
| contractType | string | 合同类型 |
| partyA | string | 甲方 |
| partyB | string | 乙方 |
| amount | number | 合同金额 |
| signDate | string | 签订日期 |
| startDate | string | 开始日期 |
| endDate | string | 结束日期 |
| fileUrl | string | 文件URL |
| fileType | string | 文件类型 |
| status | string | 状态 |
| riskLevel | string | 风险等级 |
| riskScore | number | 风险评分 |
| reviewerName | string | 审核人 |
| reviewComment | string | 审核意见 |
| uploadUserName | string | 上传人姓名 |
| uploadTime | string | 上传时间 |

**请求示例**：

```http
GET /api/contracts/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "contractNo": "CT20260706001",
    "contractName": "XX科技有限公司采购合同",
    "contractType": "采购合同",
    "partyA": "XX科技有限公司",
    "partyB": "XX供应商",
    "amount": 500000.00,
    "signDate": "2026-07-01",
    "startDate": "2026-07-01",
    "endDate": "2027-06-30",
    "fileUrl": "/files/contracts/CT20260706001.pdf",
    "fileType": "pdf",
    "status": "REVIEWING",
    "riskLevel": "HIGH",
    "riskScore": 75,
    "reviewerName": "法务主管",
    "reviewComment": "",
    "uploadUserName": "法务主管",
    "uploadTime": "2026-07-06 09:30:00"
  },
  "timestamp": 1715234567890
}
```

### 5.4 删除合同

**DELETE** `/contracts/{id}`

**使用位置**：`List.vue` - 合同列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/contracts/5
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 5.5 提交审核

**POST** `/contracts/{id}/review`

**使用位置**：`Detail.vue` - 合同详情页中点击"提交审核"按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| reviewerId | number | 是 | 审核人ID |
| reviewComment | string | 否 | 审核意见 |

**请求示例**：

```http
POST /api/contracts/1/review
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "reviewerId": 2,
  "reviewComment": "请重点审核违约责任条款"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "status": "REVIEWING",
    "reviewerId": 2,
    "reviewerName": "法务主管"
  },
  "timestamp": 1715234567890
}
```

### 5.6 审核通过

**POST** `/contracts/{id}/approve`

**使用位置**：`Detail.vue` - 合同详情页中点击"审核通过"按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| reviewComment | string | 否 | 审核意见 |

**请求示例**：

```http
POST /api/contracts/3/approve
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "reviewComment": "风险较低，审核通过"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 3,
    "status": "APPROVED",
    "reviewerName": "法务主管",
    "reviewTime": "2026-07-06 15:00:00"
  },
  "timestamp": 1715234567890
}
```

### 5.7 审核驳回

**POST** `/contracts/{id}/reject`

**使用位置**：`Detail.vue` - 合同详情页中点击"驳回"按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| reviewComment | string | 是 | 驳回理由 |

**请求示例**：

```http
POST /api/contracts/1/reject
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "reviewComment": "违约金比例过高，建议修改后重新提交"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "status": "REJECTED",
    "reviewerName": "法务主管",
    "reviewTime": "2026-07-06 16:00:00"
  },
  "timestamp": 1715234567890
}
```

### 5.8 归档合同

**POST** `/contracts/{id}/archive`

**使用位置**：`Detail.vue` - 合同详情页中点击"归档"按钮；`List.vue` - 合同列表中点击"归档"按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| archiveLocation | string | 否 | 归档位置 |
| retentionPeriod | number | 否 | 保留期限(月) |

**请求示例**：

```http
POST /api/contracts/3/archive
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "archiveLocation": "档案室A-03",
  "retentionPeriod": 36
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 3,
    "status": "ARCHIVED",
    "archiveCode": "ARC20260706001",
    "archiveTime": "2026-07-06 17:00:00"
  },
  "timestamp": 1715234567890
}
```

---

## 6. AI审核接口

### 6.1 触发AI审核

**POST** `/ai/review/{contractId}`

**使用位置**：后台自动触发（合同上传后）

**请求示例**：

```http
POST /api/ai/review/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "contractId": 1,
    "status": "PROCESSING",
    "message": "AI审核已触发，预计5分钟内完成"
  },
  "timestamp": 1715234567890
}
```

### 6.2 获取审核结果

**GET** `/ai/review/result/{contractId}`

**使用位置**：`Detail.vue` - 合同详情页中"AI审核"标签页加载时调用，展示审核结果和风险列表

响应体：

| 字段 | 类型 | 说明 |
|------|------|------|
| contractId | number | 合同ID |
| riskLevel | string | 风险等级 |
| riskScore | number | 风险评分 |
| summary.totalRisks | number | 风险总数 |
| summary.highRisks | number | 高风险数 |
| summary.mediumRisks | number | 中风险数 |
| summary.lowRisks | number | 低风险数 |
| keyInfo.partyA | string | 甲方 |
| keyInfo.partyB | string | 乙方 |
| keyInfo.amount | number | 金额 |
| keyInfo.signDate | string | 签订日期 |
| risks[] | array | 风险列表 |
| risks[].id | number | 风险ID |
| risks[].clauseNo | string | 条款编号 |
| risks[].clauseTitle | string | 条款标题 |
| risks[].clauseContent | string | 条款内容 |
| risks[].page | number | 页码 |
| risks[].riskLevel | string | 风险等级 |
| risks[].riskType | string | 风险类型 |
| risks[].riskDescription | string | 风险描述 |
| risks[].suggestion | string | 修改建议 |
| risks[].legalBasis | string | 法律依据 |
| risks[].status | string | 处理状态 |
| comparison.matching | number | 匹配条款数 |
| comparison.modified | number | 修改条款数 |
| comparison.missing | number | 缺失条款数 |
| comparison.added | number | 新增条款数 |
| conclusion | string | 审核结论 |
| reportUrl | string | 报告URL |

**请求示例**：

```http
GET /api/ai/review/result/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "contractId": 1,
    "riskLevel": "HIGH",
    "riskScore": 75,
    "summary": {
      "totalRisks": 5,
      "highRisks": 2,
      "mediumRisks": 2,
      "lowRisks": 1
    },
    "keyInfo": {
      "partyA": "XX科技有限公司",
      "partyB": "XX供应商",
      "amount": 500000.00,
      "signDate": "2026-07-01"
    },
    "risks": [
      {
        "id": 1,
        "clauseNo": "第四条",
        "clauseTitle": "违约责任",
        "clauseContent": "违约方应向守约方支付合同总额200%的违约金，无论违约情节轻重。",
        "page": 3,
        "riskLevel": "HIGH",
        "riskType": "违约金比例过高",
        "riskDescription": "违约金比例200%远超合理范围，存在显失公平风险，可能被法院调低。",
        "suggestion": "建议调整为合同总额的30%，或参照《民法典》第585条合理约定。",
        "legalBasis": "《民法典》第585条",
        "status": "PENDING"
      },
      {
        "id": 2,
        "clauseNo": "第八条",
        "clauseTitle": "知识产权归属",
        "clauseContent": "合同履行过程中产生的知识产权归乙方所有。",
        "page": 5,
        "riskLevel": "HIGH",
        "riskType": "知识产权归属不明",
        "riskDescription": "未明确约定甲方在合同履行过程中的知识产权归属，可能导致甲方无法使用相关成果。",
        "suggestion": "补充约定：甲方在合同履行过程中产生的知识产权归甲方所有，乙方享有使用权。",
        "legalBasis": "《民法典》第464条",
        "status": "PENDING"
      }
    ],
    "comparison": {
      "matching": 10,
      "modified": 4,
      "missing": 2,
      "added": 1
    },
    "conclusion": "建议修改高风险条款后重新提交",
    "reportUrl": "/reports/review-result-1.pdf"
  },
  "timestamp": 1715234567890
}
```

### 6.3 确认风险

**POST** `/risks/{id}/confirm`

**使用位置**：`Detail.vue` - AI审核结果中点击"确认风险"按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| comment | string | 否 | 确认备注 |

**请求示例**：

```http
POST /api/risks/1/confirm
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "comment": "已与对方协商，同意调整违约金比例"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "status": "ACCEPTED",
    "handleTime": "2026-07-06 14:00:00"
  },
  "timestamp": 1715234567890
}
```

### 6.4 驳回风险

**POST** `/risks/{id}/reject`

**使用位置**：`Detail.vue` - AI审核结果中点击"驳回风险"按钮

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| comment | string | 是 | 驳回理由 |

**请求示例**：

```http
POST /api/risks/2/reject
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "comment": "该条款经法务确认符合行业惯例，无需修改"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "status": "REJECTED",
    "handleTime": "2026-07-06 14:10:00"
  },
  "timestamp": 1715234567890
}
```

---

## 7. 合同批注接口

### 7.1 获取批注列表

**GET** `/comments/{contractId}`

**使用位置**：`Detail.vue` - 合同详情页中"批注"标签页加载时调用，展示批注列表

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 批注ID |
| contractId | number | 合同ID |
| userId | number | 用户ID |
| userName | string | 用户姓名 |
| userRole | string | 用户角色 |
| commentType | string | 批注类型 |
| content | string | 批注内容 |
| page | number | 页码 |
| position | string | 位置坐标 |
| status | string | 状态 |
| createTime | string | 创建时间 |

**请求示例**：

```http
GET /api/comments/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "contractId": 1,
      "userId": 2,
      "userName": "法务主管",
      "userRole": "LEGAL_MANAGER",
      "commentType": "REVIEW",
      "content": "建议修改违约责任条款，违约金比例过高。",
      "page": 3,
      "position": "{\"x\":120,\"y\":240}",
      "status": "ACTIVE",
      "createTime": "2026-07-06 11:00:00"
    },
    {
      "id": 2,
      "contractId": 1,
      "userId": 3,
      "userName": "法务专员",
      "userRole": "LEGAL_OFFICER",
      "commentType": "SUGGESTION",
      "content": "预付款比例建议降低至30%以下。",
      "page": 2,
      "position": "{\"x\":80,\"y\":160}",
      "status": "ACTIVE",
      "createTime": "2026-07-06 11:15:00"
    }
  ],
  "timestamp": 1715234567890
}
```

### 7.2 添加批注

**POST** `/comments`

**使用位置**：`Detail.vue` - 合同详情页中"批注"标签页点击"添加批注"按钮提交

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| contractId | number | 是 | 合同ID |
| commentType | string | 是 | 批注类型 |
| content | string | 是 | 批注内容 |
| page | number | 否 | 页码 |
| position | string | 否 | 位置坐标 |

**请求示例**：

```http
POST /api/comments
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "contractId": 1,
  "commentType": "SUGGESTION",
  "content": "争议解决条款建议改为甲方所在地法院管辖",
  "page": 6,
  "position": "{\"x\":200,\"y\":300}"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 4,
    "contractId": 1,
    "userName": "法务专员",
    "commentType": "SUGGESTION",
    "content": "争议解决条款建议改为甲方所在地法院管辖",
    "status": "ACTIVE",
    "createTime": "2026-07-06 14:00:00"
  },
  "timestamp": 1715234567890
}
```

### 7.3 更新批注

**PUT** `/comments/{id}`

**使用位置**：`Detail.vue` - 批注列表中点击"编辑"按钮，修改后提交

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | string | 是 | 批注内容 |
| status | string | 否 | 状态 |

**请求示例**：

```http
PUT /api/comments/4
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "content": "争议解决条款建议改为甲方所在地法院管辖（已与对方确认）",
  "status": "RESOLVED"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 4,
    "content": "争议解决条款建议改为甲方所在地法院管辖（已与对方确认）",
    "status": "RESOLVED"
  },
  "timestamp": 1715234567890
}
```

### 7.4 删除批注

**DELETE** `/comments/{id}`

**使用位置**：`Detail.vue` - 批注列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/comments/4
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 8. 合同归档接口

### 8.1 获取归档列表

**GET** `/archives`

**使用位置**：`Archive.vue` - 合同归档页面加载时调用，展示归档列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| contractNo | string | 否 | 合同编号 |
| contractName | string | 否 | 合同名称 |
| contractType | string | 否 | 合同类型 |
| archiveCode | string | 否 | 归档编号 |
| storageType | string | 否 | 存储类型 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 归档ID |
| list[].contractId | number | 合同ID |
| list[].contractNo | string | 合同编号 |
| list[].contractName | string | 合同名称 |
| list[].contractType | string | 合同类型 |
| list[].partyA | string | 甲方 |
| list[].partyB | string | 乙方 |
| list[].amount | number | 金额 |
| list[].archiveCode | string | 归档编号 |
| list[].archiveLocation | string | 归档位置 |
| list[].archiveTime | string | 归档时间 |
| list[].storageType | string | 存储类型 |
| list[].retentionPeriod | number | 保留期限 |
| list[].expireDate | string | 到期日期 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/archives?page=1&size=10&storageType=ELECTRONIC
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "contractId": 4,
        "contractNo": "CT20260705004",
        "contractName": "XX劳动合同",
        "contractType": "劳动合同",
        "partyA": "XX科技有限公司",
        "partyB": "孙七",
        "amount": 360000.00,
        "archiveCode": "ARC20260705001",
        "archiveLocation": "档案室A-03",
        "archiveTime": "2026-07-05 17:00:00",
        "storageType": "ELECTRONIC",
        "retentionPeriod": 36,
        "expireDate": "2029-07-05"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

---

## 9. 模板管理接口

### 9.1 获取模板列表

**GET** `/templates`

**使用位置**：`List.vue` - 模板列表页面加载时调用，展示模板列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| templateNo | string | 否 | 模板编号 |
| templateName | string | 否 | 模板名称 |
| contractType | string | 否 | 合同类型 |
| status | string | 否 | 状态 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 模板ID |
| list[].templateNo | string | 模板编号 |
| list[].templateName | string | 模板名称 |
| list[].contractType | string | 合同类型 |
| list[].templateVersion | string | 版本号 |
| list[].status | string | 状态 |
| list[].createdBy | string | 创建人 |
| list[].createTime | string | 创建时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/templates?page=1&size=10&contractType=采购合同
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "templateNo": "TPL-PURCHASE-001",
        "templateName": "标准采购合同模板",
        "contractType": "采购合同",
        "templateVersion": "v2.3",
        "status": "ACTIVE",
        "createdBy": "张三",
        "createTime": "2026-01-15 10:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 9.2 获取模板详情

**GET** `/templates/{id}`

**使用位置**：`List.vue` - 模板列表中点击"查看详情"按钮时调用，展示模板详细信息

响应体：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 模板ID |
| templateNo | string | 模板编号 |
| templateName | string | 模板名称 |
| contractType | string | 合同类型 |
| templateVersion | string | 版本号 |
| fileUrl | string | 文件URL |
| description | string | 模板描述 |
| applicableScope | string | 适用范围 |
| status | string | 状态 |
| clauses[] | array | 条款列表 |
| clauses[].no | string | 条款编号 |
| clauses[].title | string | 条款标题 |

**请求示例**：

```http
GET /api/templates/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "templateNo": "TPL-PURCHASE-001",
    "templateName": "标准采购合同模板",
    "contractType": "采购合同",
    "templateVersion": "v2.3",
    "fileUrl": "/files/templates/TPL-PURCHASE-001.docx",
    "description": "适用于一般采购业务的标准合同模板",
    "applicableScope": "采购金额50万元以下的一般采购业务",
    "status": "ACTIVE",
    "clauses": [
      {
        "no": "第一条",
        "title": "合同标的"
      },
      {
        "no": "第二条",
        "title": "合同价款及支付方式"
      },
      {
        "no": "第三条",
        "title": "交付及验收"
      }
    ]
  },
  "timestamp": 1715234567890
}
```

### 9.3 新增模板

**POST** `/templates`

**使用位置**：`List.vue` - 模板列表页面点击"新增模板"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| templateName | string | 是 | 模板名称 |
| contractType | string | 是 | 合同类型 |
| templateVersion | string | 是 | 版本号 |
| file | File | 是 | 模板文件 |
| description | string | 否 | 模板描述 |

**请求示例**：

```http
POST /api/templates
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="templateName"

标准采购合同模板
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="contractType"

采购合同
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="templateVersion"

v3.0
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="description"

适用于一般采购业务的标准合同模板
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file"; filename="purchase-template.docx"
Content-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document

<binary>
------WebKitFormBoundary7MA4YWxkTrZu0gW--
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 6,
    "templateNo": "TPL-PURCHASE-002",
    "templateName": "标准采购合同模板",
    "templateVersion": "v3.0",
    "status": "ACTIVE",
    "createTime": "2026-07-06 14:00:00"
  },
  "timestamp": 1715234567890
}
```

### 9.4 更新模板

**PUT** `/templates/{id}`

**使用位置**：`List.vue` - 模板列表中点击"编辑"按钮，修改后提交

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| templateName | string | 否 | 模板名称 |
| description | string | 否 | 模板描述 |
| status | string | 否 | 状态 |

**请求示例**：

```http
PUT /api/templates/1
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "templateName": "标准采购合同模板",
  "description": "适用于一般采购业务的标准合同模板（2026版）",
  "status": "ACTIVE"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "templateName": "标准采购合同模板",
    "status": "ACTIVE"
  },
  "timestamp": 1715234567890
}
```

### 9.5 删除模板

**DELETE** `/templates/{id}`

**使用位置**：`List.vue` - 模板列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/templates/5
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 10. 条款库接口

### 10.1 获取条款列表

**GET** `/clauses`

**使用位置**：`ClauseLibrary.vue` - 条款库页面加载时调用，展示条款列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| clauseName | string | 否 | 条款名称 |
| clauseCategory | string | 否 | 条款类别 |
| contractType | string | 否 | 适用合同类型 |
| riskLevel | string | 否 | 风险等级 |
| status | number | 否 | 状态 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 条款ID |
| list[].clauseCode | string | 条款编码 |
| list[].clauseName | string | 条款名称 |
| list[].clauseCategory | string | 条款类别 |
| list[].contractType | string | 适用合同类型 |
| list[].riskLevel | string | 风险等级 |
| list[].description | string | 条款描述 |
| list[].status | number | 状态 |
| list[].createTime | string | 创建时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/clauses?page=1&size=10&riskLevel=HIGH
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 4,
        "clauseCode": "CLAUSE-004",
        "clauseName": "违约责任",
        "clauseCategory": "RISK",
        "contractType": "",
        "riskLevel": "HIGH",
        "description": "违约责任条款",
        "status": 1,
        "createTime": "2026-07-01 10:00:00"
      },
      {
        "id": 6,
        "clauseCode": "CLAUSE-006",
        "clauseName": "知识产权归属",
        "clauseCategory": "SPECIAL",
        "contractType": "",
        "riskLevel": "HIGH",
        "description": "知识产权条款",
        "status": 1,
        "createTime": "2026-07-01 10:00:00"
      }
    ],
    "total": 2,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 10.2 获取条款详情

**GET** `/clauses/{id}`

**使用位置**：`ClauseLibrary.vue` - 条款列表中点击"查看详情"按钮时调用

响应体：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 条款ID |
| clauseCode | string | 条款编码 |
| clauseName | string | 条款名称 |
| clauseCategory | string | 条款类别 |
| contractType | string | 适用合同类型 |
| content | string | 条款内容 |
| standardContent | string | 标准条款内容 |
| riskLevel | string | 风险等级 |
| description | string | 条款描述 |
| legalBasis | string | 法律依据 |
| status | number | 状态 |

**请求示例**：

```http
GET /api/clauses/4
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 4,
    "clauseCode": "CLAUSE-004",
    "clauseName": "违约责任",
    "clauseCategory": "RISK",
    "contractType": "",
    "content": "违约方应向守约方支付违约金：[金额]",
    "standardContent": "违约方应向守约方支付合同总额30%以内的违约金，约定的违约金过分高于造成的损失的，当事人可以请求人民法院或者仲裁机构予以适当减少",
    "riskLevel": "HIGH",
    "description": "违约责任条款",
    "legalBasis": "《民法典》第585条",
    "status": 1
  },
  "timestamp": 1715234567890
}
```

### 10.3 新增条款

**POST** `/clauses`

**使用位置**：`ClauseLibrary.vue` - 条款库页面点击"新增条款"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clauseName | string | 是 | 条款名称 |
| clauseCode | string | 是 | 条款编码 |
| clauseCategory | string | 是 | 条款类别 |
| contractType | string | 否 | 适用合同类型 |
| content | string | 否 | 条款内容 |
| standardContent | string | 否 | 标准条款内容 |
| riskLevel | string | 否 | 风险等级 |
| description | string | 否 | 条款描述 |
| legalBasis | string | 否 | 法律依据 |

**请求示例**：

```http
POST /api/clauses
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "clauseName": "保密条款",
  "clauseCode": "CLAUSE-009",
  "clauseCategory": "COMMON",
  "contractType": "",
  "content": "双方应对合同内容及履行过程中知悉的商业秘密保密，保密期限为合同终止后3年",
  "standardContent": "双方应对合同内容及履行过程中知悉的商业秘密保密，保密期限应至少为合同终止后3年",
  "riskLevel": "MEDIUM",
  "description": "保密条款",
  "legalBasis": "《反不正当竞争法》第9条"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 9,
    "clauseCode": "CLAUSE-009",
    "clauseName": "保密条款",
    "status": 1,
    "createTime": "2026-07-06 14:00:00"
  },
  "timestamp": 1715234567890
}
```

### 10.4 更新条款

**PUT** `/clauses/{id}`

**使用位置**：`ClauseLibrary.vue` - 条款列表中点击"编辑"按钮，修改后提交

请求体：同新增条款

**请求示例**：

```http
PUT /api/clauses/4
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "clauseName": "违约责任",
  "clauseCode": "CLAUSE-004",
  "clauseCategory": "RISK",
  "content": "违约方应向守约方支付合同总额30%以内的违约金",
  "standardContent": "违约方应向守约方支付合同总额30%以内的违约金，约定的违约金过分高于造成的损失的，当事人可以请求人民法院或者仲裁机构予以适当减少",
  "riskLevel": "HIGH",
  "description": "违约责任条款",
  "legalBasis": "《民法典》第585条"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 4,
    "clauseName": "违约责任",
    "status": 1
  },
  "timestamp": 1715234567890
}
```

### 10.5 删除条款

**DELETE** `/clauses/{id}`

**使用位置**：`ClauseLibrary.vue` - 条款列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/clauses/9
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 11. 审核规则接口

### 11.1 获取规则列表

**GET** `/rules`

**使用位置**：`RuleList.vue` - 审核规则列表页面加载时调用，展示规则列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| ruleName | string | 否 | 规则名称 |
| ruleType | string | 否 | 规则类型 |
| contractType | string | 否 | 适用合同类型 |
| riskLevel | string | 否 | 风险等级 |
| status | number | 否 | 状态 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 规则ID |
| list[].ruleNo | string | 规则编号 |
| list[].ruleName | string | 规则名称 |
| list[].ruleType | string | 规则类型 |
| list[].contractType | string | 适用合同类型 |
| list[].clauseKeyword | string | 条款关键词 |
| list[].ruleConfig | object | 规则配置 |
| list[].riskLevel | string | 风险等级 |
| list[].priority | number | 优先级 |
| list[].status | number | 状态 |
| list[].description | string | 规则描述 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/rules?page=1&size=10&riskLevel=HIGH
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "ruleNo": "RULE-001",
        "ruleName": "违约金比例范围",
        "ruleType": "VALUE_RANGE",
        "contractType": "",
        "clauseKeyword": "违约金",
        "ruleConfig": {
          "min": 5,
          "max": 30,
          "unit": "%"
        },
        "riskLevel": "HIGH",
        "priority": 1,
        "status": 1,
        "description": "违约金比例不应超过合同金额的30%"
      },
      {
        "id": 3,
        "ruleNo": "RULE-003",
        "ruleName": "争议解决条款必填",
        "ruleType": "REQUIRED",
        "contractType": "",
        "clauseKeyword": "争议解决",
        "ruleConfig": {},
        "riskLevel": "HIGH",
        "priority": 1,
        "status": 1,
        "description": "合同必须包含争议解决条款"
      }
    ],
    "total": 3,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 11.2 获取规则详情

**GET** `/rules/{id}`

**使用位置**：`RuleList.vue` - 规则列表中点击"编辑"按钮时调用

响应体：同列表字段

**请求示例**：

```http
GET /api/rules/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "ruleNo": "RULE-001",
    "ruleName": "违约金比例范围",
    "ruleType": "VALUE_RANGE",
    "contractType": "",
    "clauseKeyword": "违约金",
    "ruleConfig": {
      "min": 5,
      "max": 30,
      "unit": "%"
    },
    "riskLevel": "HIGH",
    "priority": 1,
    "status": 1,
    "description": "违约金比例不应超过合同金额的30%"
  },
  "timestamp": 1715234567890
}
```

### 11.3 新增规则

**POST** `/rules`

**使用位置**：`RuleList.vue` - 规则列表页面点击"新增规则"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| ruleName | string | 是 | 规则名称 |
| ruleNo | string | 是 | 规则编号 |
| ruleType | string | 是 | 规则类型 |
| contractType | string | 否 | 适用合同类型 |
| clauseKeyword | string | 是 | 条款关键词 |
| ruleConfig | object | 否 | 规则配置(JSON) |
| riskLevel | string | 是 | 风险等级 |
| priority | number | 否 | 优先级 |
| description | string | 否 | 规则描述 |

**请求示例**：

```http
POST /api/rules
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "ruleName": "质保金比例检查",
  "ruleNo": "RULE-009",
  "ruleType": "VALUE_RANGE",
  "contractType": "采购合同",
  "clauseKeyword": "质保金",
  "ruleConfig": {
    "max": 10,
    "unit": "%"
  },
  "riskLevel": "MEDIUM",
  "priority": 2,
  "description": "质保金比例不应超过合同金额的10%"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 9,
    "ruleNo": "RULE-009",
    "ruleName": "质保金比例检查",
    "status": 1
  },
  "timestamp": 1715234567890
}
```

### 11.4 更新规则

**PUT** `/rules/{id}`

**使用位置**：`RuleList.vue` - 规则列表中点击"编辑"按钮，修改后提交

请求体：同新增规则

**请求示例**：

```http
PUT /api/rules/1
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "ruleName": "违约金比例范围",
  "ruleNo": "RULE-001",
  "ruleType": "VALUE_RANGE",
  "contractType": "",
  "clauseKeyword": "违约金",
  "ruleConfig": {
    "min": 5,
    "max": 30,
    "unit": "%"
  },
  "riskLevel": "HIGH",
  "priority": 1,
  "description": "违约金比例不应超过合同金额的30%"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "ruleName": "违约金比例范围",
    "status": 1
  },
  "timestamp": 1715234567890
}
```

### 11.5 删除规则

**DELETE** `/rules/{id}`

**使用位置**：`RuleList.vue` - 规则列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/rules/9
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 12. 风险等级接口

### 12.1 获取风险等级列表

**GET** `/risk-levels`

**使用位置**：`RuleList.vue` - 新增/编辑规则时，风险等级下拉选择框的数据来源；`Detail.vue` - AI审核结果中风险等级的显示

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 风险等级ID |
| riskCode | string | 风险代码 |
| riskName | string | 风险名称 |
| riskColor | string | 风险颜色 |
| scoreMin | number | 最低分数 |
| scoreMax | number | 最高分数 |
| description | string | 风险描述 |
| handlingSuggestion | string | 处理建议 |
| status | number | 状态 |
| sortOrder | number | 排序号 |

**请求示例**：

```http
GET /api/risk-levels
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "riskCode": "NONE",
      "riskName": "无风险",
      "riskColor": "#67C23A",
      "scoreMin": 0,
      "scoreMax": 10,
      "description": "合同条款无明显风险，符合法律法规要求",
      "handlingSuggestion": "无需修改，可直接通过审核",
      "status": 1,
      "sortOrder": 1
    },
    {
      "id": 4,
      "riskCode": "HIGH",
      "riskName": "高风险",
      "riskColor": "#E74C3C",
      "scoreMin": 61,
      "scoreMax": 85,
      "description": "合同条款存在较高风险，可能造成较大损失",
      "handlingSuggestion": "必须修改后重新提交审核",
      "status": 1,
      "sortOrder": 4
    }
  ],
  "timestamp": 1715234567890
}
```

### 12.2 获取风险等级详情

**GET** `/risk-levels/{id}`

**使用位置**：风险等级管理页面（如存在）点击"查看详情"按钮时调用

**请求示例**：

```http
GET /api/risk-levels/4
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 4,
    "riskCode": "HIGH",
    "riskName": "高风险",
    "riskColor": "#E74C3C",
    "scoreMin": 61,
    "scoreMax": 85,
    "description": "合同条款存在较高风险，可能造成较大损失",
    "handlingSuggestion": "必须修改后重新提交审核",
    "status": 1,
    "sortOrder": 4
  },
  "timestamp": 1715234567890
}
```

### 12.3 新增风险等级

**POST** `/risk-levels`

**使用位置**：风险等级管理页面（如存在）点击"新增风险等级"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| riskCode | string | 是 | 风险代码 |
| riskName | string | 是 | 风险名称 |
| riskColor | string | 否 | 风险颜色 |
| scoreMin | number | 是 | 最低分数 |
| scoreMax | number | 是 | 最高分数 |
| description | string | 否 | 风险描述 |
| handlingSuggestion | string | 否 | 处理建议 |

**请求示例**：

```http
POST /api/risk-levels
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "riskCode": "FATAL",
  "riskName": "致命风险",
  "riskColor": "#000000",
  "scoreMin": 96,
  "scoreMax": 100,
  "description": "合同条款存在致命风险，可能导致重大违法",
  "handlingSuggestion": "立即终止，禁止签署"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 6,
    "riskCode": "FATAL",
    "riskName": "致命风险",
    "sortOrder": 6
  },
  "timestamp": 1715234567890
}
```

### 12.4 更新风险等级

**PUT** `/risk-levels/{id}`

**使用位置**：风险等级管理页面（如存在）点击"编辑"按钮，修改后提交

请求体：同新增风险等级

**请求示例**：

```http
PUT /api/risk-levels/4
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "riskCode": "HIGH",
  "riskName": "高风险",
  "riskColor": "#E74C3C",
  "scoreMin": 61,
  "scoreMax": 85,
  "description": "合同条款存在较高风险，可能造成较大损失",
  "handlingSuggestion": "必须修改后重新提交审核"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 4,
    "riskName": "高风险"
  },
  "timestamp": 1715234567890
}
```

### 12.5 删除风险等级

**DELETE** `/risk-levels/{id}`

**使用位置**：风险等级管理页面（如存在）点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/risk-levels/6
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 13. 流程管理接口

### 13.1 获取流程配置列表

**GET** `/process/configs`

**使用位置**：`ProcessConfig.vue` - 流程配置页面加载时调用，展示流程配置列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| processName | string | 否 | 流程名称 |
| contractType | string | 否 | 适用合同类型 |
| status | string | 否 | 状态 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 流程配置ID |
| list[].processCode | string | 流程编码 |
| list[].processName | string | 流程名称 |
| list[].contractType | string | 适用合同类型 |
| list[].description | string | 流程描述 |
| list[].status | string | 状态 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/process/configs?page=1&size=10
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "processCode": "PROCESS-DEFAULT",
        "processName": "默认审核流程",
        "contractType": "",
        "description": "适用于所有合同类型的默认审核流程",
        "status": "ACTIVE"
      },
      {
        "id": 2,
        "processCode": "PROCESS-PURCHASE",
        "processName": "采购合同审核流程",
        "contractType": "采购合同",
        "description": "适用于采购合同的专用审核流程",
        "status": "ACTIVE"
      }
    ],
    "total": 3,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 13.2 获取流程配置详情

**GET** `/process/configs/{id}`

**使用位置**：`ProcessConfig.vue` - 流程配置列表中点击"查看节点"按钮时调用

响应体：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 流程配置ID |
| processCode | string | 流程编码 |
| processName | string | 流程名称 |
| contractType | string | 适用合同类型 |
| description | string | 流程描述 |
| status | string | 状态 |
| nodes[] | array | 流程节点列表 |

**请求示例**：

```http
GET /api/process/configs/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "processCode": "PROCESS-DEFAULT",
    "processName": "默认审核流程",
    "contractType": "",
    "description": "适用于所有合同类型的默认审核流程",
    "status": "ACTIVE",
    "nodes": [
      {
        "id": 1,
        "nodeName": "开始",
        "nodeType": "START",
        "assigneeType": "ROLE",
        "assigneeName": "",
        "sortOrder": 1
      },
      {
        "id": 2,
        "nodeName": "AI自动审核",
        "nodeType": "REVIEW",
        "assigneeType": "SYSTEM",
        "assigneeName": "系统",
        "sortOrder": 2
      },
      {
        "id": 3,
        "nodeName": "法务专员审核",
        "nodeType": "REVIEW",
        "assigneeType": "ROLE",
        "assigneeId": 3,
        "assigneeName": "法务专员",
        "sortOrder": 3
      },
      {
        "id": 4,
        "nodeName": "法务主管审批",
        "nodeType": "APPROVE",
        "assigneeType": "ROLE",
        "assigneeId": 2,
        "assigneeName": "法务主管",
        "sortOrder": 4
      }
    ]
  },
  "timestamp": 1715234567890
}
```

### 13.3 新增流程配置

**POST** `/process/configs`

**使用位置**：`ProcessConfig.vue` - 流程配置页面点击"新增流程"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| processCode | string | 是 | 流程编码 |
| processName | string | 是 | 流程名称 |
| contractType | string | 否 | 适用合同类型 |
| description | string | 否 | 流程描述 |
| nodes[] | array | 否 | 流程节点列表 |

**请求示例**：

```http
POST /api/process/configs
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "processCode": "PROCESS-SALES",
  "processName": "销售合同审核流程",
  "contractType": "销售合同",
  "description": "适用于销售合同的专用审核流程",
  "nodes": [
    {
      "nodeName": "业务审核",
      "nodeType": "REVIEW",
      "assigneeType": "ROLE",
      "assigneeId": 4,
      "sortOrder": 1
    },
    {
      "nodeName": "法务审核",
      "nodeType": "REVIEW",
      "assigneeType": "ROLE",
      "assigneeId": 3,
      "sortOrder": 2
    },
    {
      "nodeName": "法务主管审批",
      "nodeType": "APPROVE",
      "assigneeType": "ROLE",
      "assigneeId": 2,
      "sortOrder": 3
    }
  ]
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 4,
    "processCode": "PROCESS-SALES",
    "processName": "销售合同审核流程",
    "status": "ACTIVE"
  },
  "timestamp": 1715234567890
}
```

### 13.4 更新流程配置

**PUT** `/process/configs/{id}`

**使用位置**：`ProcessConfig.vue` - 流程配置列表中点击"编辑"按钮，修改后提交

请求体：同新增流程配置

**请求示例**：

```http
PUT /api/process/configs/1
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "processCode": "PROCESS-DEFAULT",
  "processName": "默认审核流程",
  "contractType": "",
  "description": "适用于所有合同类型的默认审核流程（2026版）",
  "status": "ACTIVE"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "processName": "默认审核流程",
    "status": "ACTIVE"
  },
  "timestamp": 1715234567890
}
```

### 13.5 删除流程配置

**DELETE** `/process/configs/{id}`

**使用位置**：`ProcessConfig.vue` - 流程配置列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/process/configs/4
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 13.6 获取流程节点列表

**GET** `/process/nodes`

**使用位置**：`ProcessConfig.vue` - 流程配置详情中展示节点列表

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| processId | number | 是 | 流程配置ID |

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 流程节点ID |
| processId | number | 流程配置ID |
| nodeName | string | 节点名称 |
| nodeType | string | 节点类型：START-开始节点，REVIEW-审核节点，APPROVE-审批节点，END-结束节点 |
| assigneeType | string | 办理人类型：ROLE-按角色，USER-按用户，DEPARTMENT-按部门，SYSTEM-系统 |
| assigneeId | number | 办理人ID（角色ID/用户ID/部门ID） |
| assigneeName | string | 办理人名称 |
| sortOrder | number | 排序号 |
| timeoutDays | number | 超时天数（0表示不限制） |

**请求示例**：

```http
GET /api/process/nodes?processId=1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "processId": 1,
      "nodeName": "开始",
      "nodeType": "START",
      "assigneeType": "ROLE",
      "assigneeName": "",
      "sortOrder": 1,
      "timeoutDays": 0
    },
    {
      "id": 2,
      "processId": 1,
      "nodeName": "AI自动审核",
      "nodeType": "REVIEW",
      "assigneeType": "SYSTEM",
      "assigneeName": "系统",
      "sortOrder": 2,
      "timeoutDays": 1
    },
    {
      "id": 3,
      "processId": 1,
      "nodeName": "法务专员审核",
      "nodeType": "REVIEW",
      "assigneeType": "ROLE",
      "assigneeId": 3,
      "assigneeName": "法务专员",
      "sortOrder": 3,
      "timeoutDays": 3
    }
  ],
  "timestamp": 1715234567890
}
```

### 13.7 获取流程实例列表

**GET** `/process/instances`

**使用位置**：`ProcessTracking.vue` - 流程跟踪页面加载时调用，展示流程实例列表

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| contractId | number | 否 | 合同ID |
| contractNo | string | 否 | 合同编号 |
| status | string | 否 | 状态 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 流程实例ID |
| list[].instanceCode | string | 流程实例编码 |
| list[].processName | string | 流程名称 |
| list[].contractId | number | 合同ID |
| list[].contractNo | string | 合同编号 |
| list[].status | string | 状态 |
| list[].currentNodeName | string | 当前节点 |
| list[].startTime | string | 开始时间 |
| list[].creatorName | string | 创建人 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/process/instances?page=1&size=10&status=RUNNING
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "instanceCode": "INSTANCE-20260706001",
        "processName": "默认审核流程",
        "contractId": 1,
        "contractNo": "CT20260706001",
        "status": "RUNNING",
        "currentNodeName": "法务专员审核",
        "startTime": "2026-07-06 09:30:00",
        "creatorName": "法务主管"
      },
      {
        "id": 2,
        "instanceCode": "INSTANCE-20260706002",
        "processName": "默认审核流程",
        "contractId": 2,
        "contractNo": "CT20260706002",
        "status": "RUNNING",
        "currentNodeName": "法务专员审核",
        "startTime": "2026-07-06 10:15:00",
        "creatorName": "法务专员"
      }
    ],
    "total": 2,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 13.8 获取流程实例详情

**GET** `/process/instances/{id}`

**使用位置**：`ProcessTracking.vue` - 流程实例列表中点击"查看详情"按钮时调用，展示流程审批历史

**请求示例**：

```http
GET /api/process/instances/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "instanceCode": "INSTANCE-20260706001",
    "processId": 1,
    "processName": "默认审核流程",
    "contractId": 1,
    "contractNo": "CT20260706001",
    "status": "RUNNING",
    "currentNodeId": 3,
    "currentNodeName": "法务专员审核",
    "startTime": "2026-07-06 09:30:00",
    "endTime": null,
    "creatorId": 2,
    "creatorName": "法务主管",
    "tasks": [
      {
        "id": 1,
        "taskCode": "TASK-20260706001-001",
        "nodeName": "AI自动审核",
        "assigneeName": "系统",
        "status": "COMPLETED",
        "result": "APPROVE",
        "startTime": "2026-07-06 09:30:00",
        "completeTime": "2026-07-06 09:35:00",
        "comment": "AI审核通过，风险等级：高"
      },
      {
        "id": 2,
        "taskCode": "TASK-20260706001-002",
        "nodeName": "法务专员审核",
        "assigneeName": "法务专员",
        "status": "PENDING",
        "startTime": "2026-07-06 09:35:00",
        "completeTime": null,
        "comment": null
      }
    ]
  },
  "timestamp": 1715234567890
}
```

### 13.9 获取待办任务列表

**GET** `/process/tasks/pending`

**使用位置**：`TaskAssignment.vue` - 任务分配页面加载时调用，展示待分配任务列表；`Dashboard.vue` - 工作台展示待办任务数量

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| userId | number | 否 | 用户ID |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 任务ID |
| list[].taskCode | string | 任务编码 |
| list[].nodeName | string | 节点名称 |
| list[].contractId | number | 合同ID |
| list[].contractNo | string | 合同编号 |
| list[].contractName | string | 合同名称 |
| list[].assigneeName | string | 办理人 |
| list[].status | string | 状态 |
| list[].priority | string | 优先级 |
| list[].deadline | string | 截止时间 |
| list[].startTime | string | 开始时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/process/tasks/pending?page=1&size=10&userId=3
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 2,
        "taskCode": "TASK-20260706001-002",
        "nodeName": "法务专员审核",
        "contractId": 1,
        "contractNo": "CT20260706001",
        "contractName": "XX科技有限公司采购合同",
        "assigneeName": "法务专员",
        "status": "PENDING",
        "priority": "NORMAL",
        "deadline": "2026-07-09 18:00:00",
        "startTime": "2026-07-06 09:35:00"
      },
      {
        "id": 4,
        "taskCode": "TASK-20260706002-002",
        "nodeName": "法务专员审核",
        "contractId": 2,
        "contractNo": "CT20260706002",
        "contractName": "XX服务合同",
        "assigneeName": "法务专员",
        "status": "PENDING",
        "priority": "NORMAL",
        "deadline": "2026-07-09 18:00:00",
        "startTime": "2026-07-06 10:20:00"
      }
    ],
    "total": 2,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 13.10 处理任务

**POST** `/process/tasks/{id}/handle`

**使用位置**：`Detail.vue` - 合同详情页中点击"审核通过"或"驳回"按钮时调用

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| result | string | 是 | 处理结果(APPROVE/REJECT/BACK) |
| comment | string | 否 | 处理意见 |

**请求示例**：

```http
POST /api/process/tasks/2/handle
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "result": "APPROVE",
  "comment": "已确认风险条款修改，审核通过"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "status": "COMPLETED",
    "result": "APPROVE",
    "completeTime": "2026-07-06 15:00:00",
    "nextNodeId": 4,
    "nextNodeName": "法务主管审批",
    "nextAssigneeName": "法务主管"
  },
  "timestamp": 1715234567890
}
```

### 13.11 分配任务

**POST** `/process/tasks/{id}/assign`

**使用位置**：`TaskAssignment.vue` - 任务分配页面中点击"分配"按钮时调用

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| userId | number | 是 | 用户ID |
| assignType | string | 否 | 分配类型 |

**请求示例**：

```http
POST /api/process/tasks/2/assign
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "userId": 3,
  "assignType": "PRIMARY"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "assigneeId": 3,
    "assigneeName": "法务专员",
    "assignType": "PRIMARY",
    "status": "PROCESSING"
  },
  "timestamp": 1715234567890
}
```

---

## 14. 统计报表接口

### 14.1 获取统计概览

**GET** `/statistics/overview`

**使用位置**：`Dashboard.vue` - 工作台页面加载时调用，展示统计卡片数据（合同总数、完成率、平均审核时长等）

响应体：

| 字段 | 类型 | 说明 |
|------|------|------|
| contractTotal | number | 合同总数 |
| completedRate | number | 完成率 |
| avgReviewTime | number | 平均审核时长(分钟) |
| aiAccuracy | number | AI准确率 |
| riskDistribution.high | number | 高风险数 |
| riskDistribution.medium | number | 中风险数 |
| riskDistribution.low | number | 低风险数 |
| riskDistribution.none | number | 无风险数 |

**请求示例**：

```http
GET /api/statistics/overview
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "contractTotal": 5,
    "completedRate": 40,
    "avgReviewTime": 45,
    "aiAccuracy": 92.5,
    "riskDistribution": {
      "high": 1,
      "medium": 1,
      "low": 1,
      "none": 1
    }
  },
  "timestamp": 1715234567890
}
```

### 14.2 获取风险类型统计

**GET** `/statistics/risk-types`

**使用位置**：`Dashboard.vue` - 工作台页面中风险类型饼图的数据来源

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| name | string | 风险类型名称 |
| count | number | 数量 |

**请求示例**：

```http
GET /api/statistics/risk-types
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "name": "违约金比例过高",
      "count": 1
    },
    {
      "name": "知识产权归属不明",
      "count": 1
    },
    {
      "name": "预付款比例过高",
      "count": 1
    },
    {
      "name": "保密期限过短",
      "count": 1
    },
    {
      "name": "管辖地不利",
      "count": 1
    }
  ],
  "timestamp": 1715234567890
}
```

### 14.3 获取月度趋势

**GET** `/statistics/monthly-trend`

**使用位置**：`Dashboard.vue` - 工作台页面中月度趋势折线图的数据来源

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| year | number | 否 | 年份，默认当前年 |

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| month | string | 月份 |
| count | number | 合同数量 |

**请求示例**：

```http
GET /api/statistics/monthly-trend?year=2026
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "month": "2026-01", "count": 12 },
    { "month": "2026-02", "count": 18 },
    { "month": "2026-03", "count": 25 },
    { "month": "2026-04", "count": 20 },
    { "month": "2026-05", "count": 30 },
    { "month": "2026-06", "count": 28 },
    { "month": "2026-07", "count": 5 }
  ],
  "timestamp": 1715234567890
}
```

---

## 15. 系统配置接口

### 15.1 获取配置列表

**GET** `/configs`

**使用位置**：系统配置管理页面（如存在）加载时调用，展示系统配置列表

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| configType | string | 否 | 配置类型 |

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 配置ID |
| configKey | string | 配置键 |
| configValue | string | 配置值 |
| configName | string | 配置名称 |
| configType | string | 配置类型 |
| description | string | 配置描述 |
| status | number | 状态 |

**请求示例**：

```http
GET /api/configs?configType=BUSINESS
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 3,
      "configKey": "contract.max_size",
      "configValue": "52428800",
      "configName": "合同最大上传大小",
      "configType": "BUSINESS",
      "description": "合同文件最大上传大小（字节），默认50MB",
      "status": 1
    },
    {
      "id": 4,
      "configKey": "contract.support_types",
      "configValue": "pdf,doc,docx",
      "configName": "支持的文件类型",
      "configType": "BUSINESS",
      "description": "支持上传的合同文件类型",
      "status": 1
    },
    {
      "id": 5,
      "configKey": "ai.review.enabled",
      "configValue": "true",
      "configName": "AI审核启用",
      "configType": "BUSINESS",
      "description": "是否启用AI自动审核功能",
      "status": 1
    }
  ],
  "timestamp": 1715234567890
}
```

### 15.2 获取配置详情

**GET** `/configs/{id}`

**使用位置**：系统配置管理页面（如存在）点击"查看详情"按钮时调用

**请求示例**：

```http
GET /api/configs/3
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 3,
    "configKey": "contract.max_size",
    "configValue": "52428800",
    "configName": "合同最大上传大小",
    "configType": "BUSINESS",
    "description": "合同文件最大上传大小（字节），默认50MB",
    "status": 1
  },
  "timestamp": 1715234567890
}
```

### 15.3 新增配置

**POST** `/configs`

**使用位置**：系统配置管理页面（如存在）点击"新增配置"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| configKey | string | 是 | 配置键 |
| configValue | string | 是 | 配置值 |
| configName | string | 是 | 配置名称 |
| configType | string | 否 | 配置类型 |
| description | string | 否 | 配置描述 |

**请求示例**：

```http
POST /api/configs
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "configKey": "contract.review.timeout",
  "configValue": "48",
  "configName": "审核超时时间",
  "configType": "BUSINESS",
  "description": "人工审核超时时间（小时）"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 9,
    "configKey": "contract.review.timeout",
    "configValue": "48",
    "configName": "审核超时时间"
  },
  "timestamp": 1715234567890
}
```

### 15.4 更新配置

**PUT** `/configs/{id}`

**使用位置**：系统配置管理页面（如存在）点击"编辑"按钮，修改后提交

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| configValue | string | 是 | 配置值 |
| status | number | 否 | 状态 |

**请求示例**：

```http
PUT /api/configs/3
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "configValue": "104857600",
  "status": 1
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 3,
    "configKey": "contract.max_size",
    "configValue": "104857600",
    "status": 1
  },
  "timestamp": 1715234567890
}
```

### 15.5 删除配置

**DELETE** `/configs/{id}`

**使用位置**：系统配置管理页面（如存在）点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/configs/9
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

---

## 16. 数据备份接口

### 16.1 获取备份列表

**GET** `/backups`

**使用位置**：数据备份管理页面（如存在）加载时调用，展示备份列表

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| backupType | string | 否 | 备份类型 |
| backupStatus | string | 否 | 备份状态 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 备份ID |
| list[].backupName | string | 备份名称 |
| list[].backupType | string | 备份类型 |
| list[].backupPath | string | 备份路径 |
| list[].backupSize | number | 文件大小 |
| list[].backupStatus | string | 备份状态 |
| list[].backupTime | string | 备份时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/backups?page=1&size=10
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "backupName": "2026-07-06 全量备份",
        "backupType": "FULL",
        "backupPath": "/backups/full-20260706.sql",
        "backupSize": 15728640,
        "backupStatus": "SUCCESS",
        "backupTime": "2026-07-06 02:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 16.2 创建备份

**POST** `/backups`

**使用位置**：数据备份管理页面（如存在）点击"创建备份"按钮提交

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| backupType | string | 是 | 备份类型 |
| backupName | string | 否 | 备份名称 |
| description | string | 否 | 备份描述 |

**请求示例**：

```http
POST /api/backups
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "backupType": "FULL",
  "backupName": "2026-07-06 手动全量备份",
  "description": "系统升级前手动备份"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "backupName": "2026-07-06 手动全量备份",
    "backupType": "FULL",
    "backupStatus": "PENDING",
    "message": "备份任务已创建，正在执行中"
  },
  "timestamp": 1715234567890
}
```

### 16.3 删除备份

**DELETE** `/backups/{id}`

**使用位置**：数据备份管理页面（如存在）点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/backups/2
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 16.4 恢复备份

**POST** `/backups/{id}/restore`

**使用位置**：数据备份管理页面（如存在）点击"恢复"按钮，确认后恢复

**请求示例**：

```http
POST /api/backups/1/restore
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "backupName": "2026-07-06 全量备份",
    "status": "RESTORING",
    "message": "数据恢复任务已启动，预计5分钟内完成"
  },
  "timestamp": 1715234567890
}
```

---

## 17. 操作日志接口

### 17.1 获取日志列表

**GET** `/logs`

**使用位置**：`OperationLog.vue` - 操作日志页面加载时调用，展示操作日志列表表格

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| userId | number | 否 | 用户ID |
| operationType | string | 否 | 操作类型 |
| module | string | 否 | 操作模块 |
| startTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 日志ID |
| list[].userId | number | 用户ID |
| list[].userName | string | 用户姓名 |
| list[].operationType | string | 操作类型 |
| list[].module | string | 操作模块 |
| list[].description | string | 操作描述 |
| list[].requestUrl | string | 请求URL |
| list[].ipAddress | string | IP地址 |
| list[].success | number | 是否成功 |
| list[].errorMessage | string | 错误信息 |
| list[].createTime | string | 操作时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/logs?page=1&size=10&operationType=LOGIN
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "userId": 1,
        "userName": "管理员",
        "operationType": "LOGIN",
        "module": "用户管理",
        "description": "用户登录",
        "requestUrl": "/api/user/login",
        "ipAddress": "192.168.1.100",
        "success": 1,
        "errorMessage": null,
        "createTime": "2026-07-06 09:00:00"
      },
      {
        "id": 2,
        "userId": 2,
        "userName": "法务主管",
        "operationType": "CREATE",
        "module": "合同管理",
        "description": "上传合同",
        "requestUrl": "/api/contracts/upload",
        "ipAddress": "192.168.1.101",
        "success": 1,
        "errorMessage": null,
        "createTime": "2026-07-06 09:30:00"
      }
    ],
    "total": 2,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

---

## 18. OA对接接口

### 18.1 获取OA配置列表

**GET** `/integration/oa`

**使用位置**：`OADocking.vue` - OA对接页面加载时调用，展示OA配置列表

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | OA对接ID |
| oaType | string | OA类型 |
| oaName | string | OA名称 |
| status | string | 状态 |
| syncStatus | string | 同步状态 |
| lastSyncTime | string | 最后同步时间 |

**请求示例**：

```http
GET /api/integration/oa
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "oaType": "WECOM",
      "oaName": "企业微信对接",
      "status": "CONNECTED",
      "syncStatus": "ENABLED",
      "lastSyncTime": "2026-07-06 08:00:00"
    }
  ],
  "timestamp": 1715234567890
}
```

### 18.2 获取OA配置详情

**GET** `/integration/oa/{id}`

**使用位置**：`OADocking.vue` - OA配置列表中点击"查看详情"按钮时调用

**请求示例**：

```http
GET /api/integration/oa/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "oaType": "WECOM",
    "oaName": "企业微信对接",
    "status": "CONNECTED",
    "appId": "wwxxxxxxxxxxxx",
    "syncStatus": "ENABLED",
    "lastSyncTime": "2026-07-06 08:00:00",
    "description": "企业微信消息推送和审批同步"
  },
  "timestamp": 1715234567890
}
```

### 18.3 新增OA配置

**POST** `/integration/oa`

**使用位置**：`OADocking.vue` - OA对接页面点击"新增配置"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| oaType | string | 是 | OA类型 |
| oaName | string | 是 | OA名称 |
| appId | string | 否 | 应用ID |
| appSecret | string | 否 | 应用密钥 |

**请求示例**：

```http
POST /api/integration/oa
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "oaType": "DINGTALK",
  "oaName": "钉钉对接",
  "appId": "dingxxxxxxxxxxxx",
  "appSecret": "xxxxxxxxxxxxxxxx"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "oaType": "DINGTALK",
    "oaName": "钉钉对接",
    "status": "DISCONNECTED"
  },
  "timestamp": 1715234567890
}
```

### 18.4 更新OA配置

**PUT** `/integration/oa/{id}`

**使用位置**：`OADocking.vue` - OA配置列表中点击"编辑"按钮，修改后提交

请求体：同新增OA配置

**请求示例**：

```http
PUT /api/integration/oa/1
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "oaType": "WECOM",
  "oaName": "企业微信对接",
  "appId": "wwxxxxxxxxxxxx",
  "appSecret": "xxxxxxxxxxxxxxxx"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "oaName": "企业微信对接",
    "status": "CONNECTED"
  },
  "timestamp": 1715234567890
}
```

### 18.5 删除OA配置

**DELETE** `/integration/oa/{id}`

**使用位置**：`OADocking.vue` - OA配置列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/integration/oa/2
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 18.6 获取OA对接日志

**GET** `/integration/oa/{id}/logs`

**使用位置**：`OADocking.vue` - OA对接日志部分，展示对接日志列表

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 日志ID |
| list[].logType | string | 日志类型 |
| list[].action | string | 操作动作 |
| list[].status | string | 状态 |
| list[].errorMessage | string | 错误信息 |
| list[].createTime | string | 创建时间 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/integration/oa/1/logs?page=1&size=10
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "logType": "SYNC",
        "action": "审批结果推送",
        "status": "SUCCESS",
        "errorMessage": null,
        "createTime": "2026-07-06 08:00:00"
      },
      {
        "id": 2,
        "logType": "MESSAGE",
        "action": "待办消息推送",
        "status": "SUCCESS",
        "errorMessage": null,
        "createTime": "2026-07-06 09:35:00"
      }
    ],
    "total": 2,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 18.7 同步OA数据

**POST** `/integration/oa/{id}/sync`

**使用位置**：`OADocking.vue` - OA对接页面点击"同步数据"按钮时调用

**请求示例**：

```http
POST /api/integration/oa/1/sync
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "syncStatus": "ENABLED",
    "lastSyncTime": "2026-07-06 14:00:00",
    "message": "数据同步完成，共处理 12 条记录"
  },
  "timestamp": 1715234567890
}
```

---

## 19. 电子签章接口

### 19.1 获取签章配置列表

**GET** `/integration/sign`

**使用位置**：`ElectronicSignature.vue` - 电子签章页面加载时调用，展示签章配置列表

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 电子签章ID |
| signType | string | 签章类型 |
| signName | string | 签章名称 |
| status | string | 状态 |
| certificateExpireDate | string | 证书到期日期 |

**请求示例**：

```http
GET /api/integration/sign
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "signType": "CA",
      "signName": "CA证书电子签章",
      "status": "ACTIVE",
      "certificateExpireDate": "2027-12-31"
    }
  ],
  "timestamp": 1715234567890
}
```

### 19.2 获取签章配置详情

**GET** `/integration/sign/{id}`

**使用位置**：`ElectronicSignature.vue` - 签章配置列表中点击"查看证书"按钮时调用

**请求示例**：

```http
GET /api/integration/sign/1
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "signType": "CA",
    "signName": "CA证书电子签章",
    "status": "ACTIVE",
    "appId": "CA-001",
    "certificatePath": "/certs/ca-001.pfx",
    "certificateNumber": "CA-2026-001",
    "certificateExpireDate": "2027-12-31",
    "description": "基于CA证书的电子签章服务"
  },
  "timestamp": 1715234567890
}
```

### 19.3 新增签章配置

**POST** `/integration/sign`

**使用位置**：`ElectronicSignature.vue` - 电子签章页面点击"新增配置"按钮提交表单

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| signType | string | 是 | 签章类型 |
| signName | string | 是 | 签章名称 |
| appId | string | 否 | 应用ID |
| appKey | string | 否 | 应用密钥 |
| certificatePath | string | 否 | 证书路径 |

**请求示例**：

```http
POST /api/integration/sign
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "signType": "ALIPAY",
  "signName": "支付宝电子签章",
  "appId": "2021001xxxxx",
  "appKey": "xxxxxxxxxxxxxxxx"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "signType": "ALIPAY",
    "signName": "支付宝电子签章",
    "status": "ACTIVE"
  },
  "timestamp": 1715234567890
}
```

### 19.4 更新签章配置

**PUT** `/integration/sign/{id}`

**使用位置**：`ElectronicSignature.vue` - 签章配置列表中点击"编辑"按钮，修改后提交

请求体：同新增签章配置

**请求示例**：

```http
PUT /api/integration/sign/1
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "signType": "CA",
  "signName": "CA证书电子签章",
  "appId": "CA-001",
  "appKey": "xxxxxxxx",
  "certificatePath": "/certs/ca-001.pfx"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "signName": "CA证书电子签章",
    "status": "ACTIVE"
  },
  "timestamp": 1715234567890
}
```

### 19.5 删除签章配置

**DELETE** `/integration/sign/{id}`

**使用位置**：`ElectronicSignature.vue` - 签章配置列表中点击"删除"按钮，确认后删除

**请求示例**：

```http
DELETE /api/integration/sign/2
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": true,
  "timestamp": 1715234567890
}
```

### 19.6 获取签章记录列表

**GET** `/integration/sign/records`

**使用位置**：`ElectronicSignature.vue` - 签章记录列表部分，展示历史签章记录

请求参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | number | 否 | 页码 |
| size | number | 否 | 每页条数 |
| contractId | number | 否 | 合同ID |
| verifyStatus | string | 否 | 验证状态 |

响应体（分页）：

| 字段 | 类型 | 说明 |
|------|------|------|
| list[].id | number | 签章记录ID |
| list[].contractId | number | 合同ID |
| list[].contractNo | string | 合同编号 |
| list[].signTime | string | 签章时间 |
| list[].signUserName | string | 签章人 |
| list[].signatureImage | string | 签章图片URL |
| list[].verifyStatus | string | 验证状态 |
| total | number | 总数 |

**请求示例**：

```http
GET /api/integration/sign/records?page=1&size=10&verifyStatus=VERIFIED
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "contractId": 4,
        "contractNo": "CT20260705004",
        "signTime": "2026-07-05 16:50:00",
        "signUserName": "管理员",
        "signatureImage": "/files/signatures/sig-1.png",
        "verifyStatus": "VERIFIED"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  },
  "timestamp": 1715234567890
}
```

### 19.7 签署合同

**POST** `/integration/sign/{contractId}`

**使用位置**：`Detail.vue` - 合同详情页中点击"签署合同"按钮时调用

请求体：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| signId | number | 是 | 电子签章ID |
| signPosition | string | 否 | 签章位置 |

**请求示例**：

```http
POST /api/integration/sign/4
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx

{
  "signId": 1,
  "signPosition": "{\"page\":5,\"x\":300,\"y\":400}"
}
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "recordId": 2,
    "contractId": 4,
    "signTime": "2026-07-06 14:00:00",
    "verifyStatus": "PENDING",
    "message": "签章已提交，等待验证"
  },
  "timestamp": 1715234567890
}
```

---

## 20. API开放接口

### 20.1 获取API配置列表

**GET** `/integration/api`

**使用位置**：`API开放.vue` - API开放页面加载时调用，展示API配置列表

响应体（数组）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | API配置ID |
| apiName | string | API名称 |
| apiPath | string | API路径 |
| httpMethod | string | HTTP方法：GET、POST、PUT、DELETE |
| apiVersion | string | API版本号 |
| status | string | 状态：ACTIVE-启用，INACTIVE-禁用 |
| authType | string | 认证类型：NONE、API_KEY、TOKEN、OAUTH2 |
| rateLimit | number | 限流次数（次/分钟，0表示不限流） |
| description | string | 描述 |
| createTime | string | 创建时间 |

**请求示例**：

```http
GET /api/integration/api
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxxx
```

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "apiName": "合同查询API",
      "apiPath": "/api/v1/contracts",
      "httpMethod": "GET",
      "apiVersion": "1.0",
      "status": "ACTIVE",
      "authType": "API_KEY",
      "rateLimit": 100,
      "description": "查询合同列表",
      "createTime": "2026-08-25 10:00:00"
    },
    {
      "id": 2,
      "apiName": "合同详情API",
      "apiPath": "/api/v1/contracts/{id}",
      "httpMethod": "GET",
      "apiVersion": "1.0",
      "status": "ACTIVE",
      "authType": "API_KEY",
      "rateLimit": 100,
      "description": "查询合同详情",
      "createTime": "2026-08-25 10:00:00"
    },
    {
      "id": 3,
      "apiName": "合同上传API",
      "apiPath": "/api/v1/contracts/upload",
      "httpMethod": "POST",
      "apiVersion": "1.0",
      "status": "ACTIVE",
      "authType": "API_KEY",
      "rateLimit": 50,
      "description": "上传合同文件",
      "createTime": "2026-08-25 10:00:00"
    }
  ],
  "timestamp": 1715234567890
}
```

---

> 文档版本：v1.0  
> 最后更新：2026-08-25  
> 维护人：智能合同审批系统开发团队